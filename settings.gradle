pluginManagement {
    repositories {
        google() // <--- THIS IS CRUCIAL
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google() // <--- Also crucial for dependencies
        mavenCentral()
    }
}

rootProject.name = "LANChat"
include ':app'